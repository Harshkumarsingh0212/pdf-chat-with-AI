{
  "name": "chat with pdfs",
  "version": "1.0.0",
  "description": "",
  "main": "react/pdf-qa/src/App.j",
  "scripts": {
    "build": "cd fastapi && uvicorn main:app --host 0.0.0.0 --port 8000 && cd ../react/pdf-qa && npm install && npm run build",
    "start": "cd react/pdf-qa && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
},
  "dependencies": {
    "axios": "^1.6.8"
  }
}
